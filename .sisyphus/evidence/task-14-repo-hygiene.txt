Repository hygiene verification

.gitignore check:
- `.sisyphus/` present exactly once in root `.gitignore`.

AGENTS.md section check:
- Present sections: <repo_map>, <execution_commands>, <qa_strategy>, <commit_policy>, <semantic_markup>, <context_index>, <guardrails>.

Context marker grep check:
- `grep "<context:"` found 20 matches in 18 files.
- Examples: `backend/src/main.ts`, `backend/prisma/schema.prisma`, `frontend/src/lib/api.ts`, `README.md`.

Git status check:
- `git status --short` shows tracked working tree edits only and no `.sisyphus/` entries.

Build command summaries:
- `cd backend && npm run build` failed because `nest` is not installed in this environment (`sh: nest: command not found`).
- `cd frontend && npm run build` failed because `vite` is not installed in this environment (`sh: vite: command not found`).
